# Scoratis Backend Dependencies
# Tested and compatible versions

# Core FastAPI
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-dotenv==1.0.1
python-multipart>=0.0.6

# Pydantic (compatible versions)
pydantic==2.10.0
pydantic-settings==2.6.0

# HTTP client
httpx==0.28.0

# Google APIs (for YouTube)
google-generativeai==0.8.0
google-api-python-client==2.150.0

# PostgreSQL + SQLAlchemy
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
psycopg[binary]>=3.1.0
asyncpg==0.30.0
alembic==1.14.0

# pgvector for RAG
pgvector==0.3.6

# Embeddings
sentence-transformers==3.3.1

# Web Search
duckduckgo-search==6.3.7

# TTS
# Unofficial wrapper around Microsoft Edge's internal TTS API - it embeds a
# "trusted client token" that Microsoft periodically rotates, which breaks
# older pinned versions with a 403 on the websocket handshake (confirmed:
# 6.1.18 was stale, 7.2.8 works). Use a floor, not an exact pin, so this
# doesn't silently go stale again.
edge-tts>=7.2.0

# Video generation - tasks_pkg/video_tasks.py shells out to `python -m manim`,
# services/agent/tools.py imports it directly. Requires the system ffmpeg +
# LaTeX packages installed in backend/Dockerfile.
manim>=0.18.0
pydub>=0.25.0
ffmpeg-python>=0.2.0

# Utilities
# manim>=0.18 requires numpy>=2.1; verified sentence-transformers, scipy,
# pgvector, and langgraph all import cleanly under numpy 2.5.
numpy>=2.1.0
scipy>=1.11.0
lxml>=5.0.0

# LangGraph for conversation state management
langgraph>=0.2.0
langgraph-checkpoint-postgres>=1.0.0

# LiteLLM - Universal LLM adapter
litellm>=1.52.0

# Encryption for API keys
cryptography>=42.0.0

# Authentication (JWT + password hashing)
pyjwt>=2.9.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
email-validator>=2.1.0

# === NEW: Professional RAG System Dependencies ===

# Celery + Redis for background task processing
celery>=5.3.0
redis>=5.0.0

# Document parsing (simplified - avoiding heavy unstructured deps)
pypdf>=4.0.0
python-docx>=1.1.0
python-magic>=0.4.27
beautifulsoup4>=4.12.0
markdown>=3.5.0

# Text chunking
langchain-text-splitters>=0.2.0

# File type detection
aiofiles>=24.1.0

# === Observability ===
langsmith>=0.1.0

# === Testing Dependencies ===
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
httpx>=0.28.0
factory-boy>=3.3.0
faker>=22.0.0
